sudo: false

services: docker

language: cpp

stages:
 - build 
 - test
 - deploy

jobs:
  include:
  - stage: build & test
    script: 
      - docker-compose build 
      - docker run selfaware-monopoly-server ./ci_test.sh

  - stage: deploy
    script: echo " ... deploy ... "

branches:
  only:
    - master
