sudo: false

services: docker

language: cpp

stages:
 - build 
 - test
 - deploy

jobs:
  include:
  - stage: build
    script: docker-compose build

  - stage: test
    script: 
      - docker run selfaware-monopoly-server ./ci_test.sh

  - stage: deploy
    script: echo " ... deploy ... "

branches:
  only:
    - master
