sudo: false

services: docker

language: cpp

stages:
 - build 
 - test
 - deploy

jobs:
  include:
  - stage: build
    script: docker-compose build

  - stage: test
    script: 
      - ./Server/test
      - ./client/test
  - stage: deploy
    script: echo " ... deploy ... "

branches:
  only:
    - master
